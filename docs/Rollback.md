# Rollback (Safety Net)

Plans how to undo a change if it breaks something (which commit to revert to, which files to restore).

## Current Rollback Plan
- Changes made so far are purely documentation (`docs/` folder initialization).
- Rollback procedure: delete the `docs/` folder if it was created by mistake.
- As the project advances, proper git version control rollback steps will be documented here.
